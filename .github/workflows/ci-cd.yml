name: CI

on:
  push:
    branches: [ "master" ]

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff pytest httpx starlette

      - name: Lint
        run: ruff check .

      - name: Run tests
        env:
          ENVIRONMENT: "dev"
        run: pytest

  build:
    name: Build and push image
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      tag: ${{ steps.meta.outputs.tag }}

    steps:
      - uses: actions/checkout@v4

      - name: Set image metadata
        id: meta
        run: |
          TAG="${GITHUB_SHA}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tag }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  security_scan:
    name: Security scan
    needs: build
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      contents: read
      actions: read

    steps:
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_NAME }}:${{ needs.build.outputs.tag }}
          format: sarif
          output: trivy-results.sarif
          vuln-type: "os,library"
          severity: "CRITICAL,HIGH"

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-results.sarif

  deploy_dev:
    name: Deploy to development
    needs: security_scan
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select image tag to deploy
        id: meta
        run: |
          echo "tag=${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Deploy
        env:
          SSH_HOST: ${{ secrets.PROD_SSH_HOST }}
          SSH_USER: ${{ secrets.PROD_SSH_USER }}
          SSH_KEY:  ${{ secrets.PROD_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" | base64 --decode > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

          scp -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no docker-compose.dev.yml $SSH_USER@$SSH_HOST:/opt/actions-test
          scp -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no caddy/Caddyfile.dev $SSH_USER@$SSH_HOST:/opt/actions-test
          scp -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no deploy/deploy.sh $SSH_USER@$SSH_HOST:/opt/actions-test

          ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST << EOF
            cd /opt/actions-test
            chmod u+x deploy.sh
            export TAG=${{ env.tag }}
            ./deploy.sh
          EOF

  deploy_prod:
    name: Deploy to production
    needs: deploy_dev
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select image tag to deploy
        id: meta
        run: |
          echo "tag=${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Deploy
        env:
          SSH_HOST: ${{ secrets.PROD_SSH_HOST }}
          SSH_USER: ${{ secrets.PROD_SSH_USER }}
          SSH_KEY:  ${{ secrets.PROD_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" | base64 --decode > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

          scp -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no docker-compose.prod.yml $SSH_USER@$SSH_HOST:/opt/actions-test
          scp -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no caddy/Caddyfile.prod $SSH_USER@$SSH_HOST:/opt/actions-test
          scp -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no deploy/deploy.sh $SSH_USER@$SSH_HOST:/opt/actions-test

          ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST << EOF
            export IMAGE=${{ env.IMAGE_NAME }}
            export TAG=${{ env.tag }}
            export ENV=prod
            export USER=${{ github.actor }}
            export TOKEN=${{ secrets.GITHUB_TOKEN }}
            cd /opt/actions-test
            chmod u+x deploy.sh
            ./deploy.sh
          EOF
